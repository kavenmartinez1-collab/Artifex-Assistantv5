# ─────────────────────────────────────────────────────────────────────
#  Artifex Assistant V5 — Pinned deps for RTX 5060 Ti (8 GB VRAM)
# ─────────────────────────────────────────────────────────────────────
#
#  GPU:   NVIDIA RTX 5060 Ti 8 GB (Blackwell, sm_120)
#  CUDA:  12.8+ (bundled with PyTorch)
#  Python: 3.11+
#
#  Single-command install (torch wheel pulled from the cu128 index;
#  Blackwell sm_120 first appears in torch 2.7):
#    pip install -r requirements-rtx5060ti.txt
# ─────────────────────────────────────────────────────────────────────

# ── PyTorch (CUDA 12.8 wheels — Blackwell) ────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.7.1
torchvision==0.22.1
torchaudio==2.7.1

# ── Core AI (pinned — no auto-updates) ────────────────────────────────
transformers==5.3.0
bitsandbytes==0.49.2
accelerate==1.6.0
numpy==2.3.5
safetensors==0.7.0
huggingface-hub==1.7.2
tiktoken==0.12.0

# ── Image / 3D / Video generation ────────────────────────────────────
diffusers==0.30.0
Pillow==12.0.0
imageio==2.37.3
imageio-ffmpeg==0.6.0

# ── GUI (PyQt6) ──────────────────────────────────────────────────────
PyQt6==6.11.0
PyQt6-Qt6==6.11.0
PyQt6-sip==13.11.1
pyqtgraph==0.14.0      # 3D forward-pass telemetry panel (Forward Pass tab)
PyOpenGL==3.1.10       # OpenGL backend for pyqtgraph

# ── Application ──────────────────────────────────────────────────────
colorama==0.4.6
pyperclip==1.11.0
psutil==7.2.2
duckduckgo-search==8.1.1
pypdf==6.9.1
lxml==6.0.2
sounddevice==0.5.1
scipy==1.15.3
soundfile==0.13.1
librosa==0.11.0
trimesh==4.11.5

# ── Voice Assistant ──────────────────────────────────────────────────
faster-whisper==1.2.1
pynput==1.8.1
piper-tts==1.4.2

# ── API server / Web gateway ─────────────────────────────────────────
fastapi==0.135.1
uvicorn==0.42.0
httpx==0.28.1
slowapi==0.1.9
