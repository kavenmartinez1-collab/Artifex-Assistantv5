# ─────────────────────────────────────────────────────────────────────
#  Artifex Assistant — Pinned dependencies for RTX 3060 (12 GB VRAM)
# ─────────────────────────────────────────────────────────────────────
#
#  GPU:   NVIDIA RTX 3060 12 GB (Ampere, sm_86, compute capability 8.6)
#  CUDA:  12.4 (bundled with PyTorch — no separate CUDA toolkit needed)
#  Python: 3.11.x
#
#  IMPORTANT: Do NOT install this file directly!
#  Use setup_3060.bat — it installs torch from the correct index first,
#  then installs these remaining dependencies.
#
# ─────────────────────────────────────────────────────────────────────

# ── Model loading / quantization ────────────────────────────────────
# transformers 5.2.0: first stable release with full Qwen3.5 support
#   (architecture merged 2026-02-09 in PR #43830)
# bitsandbytes 0.49.1: NF4 + double-quant, CUDA 11.8-13.0 compat
# accelerate 1.12.0: device_map placement, memory budgeting
transformers==5.2.0
bitsandbytes==0.49.1
accelerate==1.12.0
numpy>=1.26.0,<3.0.0
huggingface-hub>=1.3.0,<2.0.0

# ── Application dependencies ────────────────────────────────────────
colorama==0.4.6
duckduckgo-search>=7.0.0,<9.0.0
pypdf>=4.0.0,<6.0.0
lxml>=5.2.1,<6.0.0
psutil>=5.9.0,<7.0.0
FreeSimpleGUI>=5.0.0,<6.0.0
pyperclip>=1.9.0,<2.0.0
