# ─────────────────────────────────────────────────────────────────────
#  Artifex Assistant V5 — Pinned dependencies (supply-chain safe)
# ─────────────────────────────────────────────────────────────────────
#
#  All versions pinned to exact releases. No auto-updates.
#  Install PyTorch with CUDA first (not listed here — GPU-specific):
#
#    RTX 30xx / 40xx:
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
#    RTX 50xx (Blackwell):
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
#  Then:
#    pip install -r requirements.txt
#
#  For GPU-specific pinned versions (including torch), see:
#    requirements-3060.txt       — RTX 3060 12 GB
#    requirements-rtx4090.txt    — RTX 4090 24 GB
#    requirements-rtx5060ti.txt  — RTX 5060 Ti 8 GB
# ─────────────────────────────────────────────────────────────────────

# ── Core AI ───────────────────────────────────────────────────────────
transformers==5.3.0
bitsandbytes==0.49.2
accelerate==1.6.0
numpy==2.3.5
safetensors==0.7.0

# ── Model hub / tokenizers ───────────────────────────────────────────
huggingface-hub==1.7.2
tiktoken==0.12.0

# ── Image / 3D / Video generation ────────────────────────────────────
diffusers==0.30.0
Pillow==12.0.0
imageio==2.37.3
imageio-ffmpeg==0.6.0

# ── GUI (PyQt6) ──────────────────────────────────────────────────────
PyQt6==6.11.0
PyQt6-Qt6==6.11.0
PyQt6-sip==13.11.1

# ── Application ──────────────────────────────────────────────────────
colorama==0.4.6
FreeSimpleGUI==5.2.0.post1
pyperclip==1.11.0
psutil==7.2.2
duckduckgo-search==8.1.1
pypdf==6.9.1
lxml==6.0.2

# ── Audio I/O ────────────────────────────────────────────────────────
sounddevice==0.5.1
scipy==1.15.3
soundfile==0.13.1
librosa==0.11.0

# ── 3D mesh export ───────────────────────────────────────────────────
trimesh==4.11.5

# ── API server ───────────────────────────────────────────────────────
fastapi==0.135.1
uvicorn==0.42.0

# ── Web gateway ──────────────────────────────────────────────────────
httpx==0.28.1
slowapi==0.1.9
