# ─────────────────────────────────────────────────────────────────────
#  Artifex Assistant V5 — Pinned dependencies (supply-chain safe)
# ─────────────────────────────────────────────────────────────────────
#
#  All versions pinned to exact releases. No auto-updates.
#
#  This file targets RTX 30xx / 40xx (CUDA 12.4 wheels). For Blackwell
#  (RTX 50xx) you MUST use requirements-rtx5060ti.txt instead — it pins
#  torch from the cu128 index, which is required for sm_120 support.
#
#  Single-command install:
#    pip install -r requirements.txt
#
#  GPU-specific files (each is a complete reproducible install):
#    requirements-3060.txt       — RTX 3060 12 GB     (cu124, torch 2.6.0)
#    requirements-rtx4090.txt    — RTX 4090 24 GB     (cu124, torch 2.6.0)
#    requirements-rtx5060ti.txt  — RTX 5060 Ti 8 GB   (cu128, torch 2.7.1)
# ─────────────────────────────────────────────────────────────────────

# ── PyTorch (CUDA 12.4 wheels — RTX 30xx / 40xx) ──────────────────────
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0

# ── Core AI ───────────────────────────────────────────────────────────
transformers==5.3.0
bitsandbytes==0.49.2
accelerate==1.6.0
numpy==2.3.5
safetensors==0.7.0

# ── Model hub / tokenizers ───────────────────────────────────────────
huggingface-hub==1.7.2
tiktoken==0.12.0

# ── Image / 3D / Video generation ────────────────────────────────────
diffusers==0.30.0
opencv-python-headless==5.0.0.93  # photo restoration (classical clean stage)
Pillow==12.0.0
imageio==2.37.3
imageio-ffmpeg==0.6.0

# ── GUI (PyQt6) ──────────────────────────────────────────────────────
PyQt6==6.11.0
PyQt6-Qt6==6.11.0
PyQt6-sip==13.11.1
pyqtgraph==0.14.0      # 3D forward-pass telemetry panel (Forward Pass tab)
PyOpenGL==3.1.10       # OpenGL backend for pyqtgraph

# ── Application ──────────────────────────────────────────────────────
colorama==0.4.6
pyperclip==1.11.0
psutil==7.2.2
duckduckgo-search==8.1.1
pypdf==6.9.1
lxml==6.0.2

# ── Audio I/O ─────────────────────────────────────────────���──────────
sounddevice==0.5.1
scipy==1.15.3
soundfile==0.13.1
librosa==0.11.0

# ── Voice Assistant ──────────────────────────────────────────────────
faster-whisper==1.2.1
pynput==1.8.1
piper-tts==1.4.2

# ── 3D mesh export ───────────────────────────────────────────────────
trimesh==4.11.5

# ── API server ───────────────────────────────────────────────────────
fastapi==0.135.1
uvicorn==0.42.0

# ── Web gateway ──────────────────────────────────────────────────────
httpx==0.28.1
slowapi==0.1.9
